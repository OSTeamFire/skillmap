services:
  skillmap:
    build: .
    volumes:
      - ./output:/app/output
    command: >
      --url "${SEED_URL:-https://www.getonbrd.com/empleos-ingeniero-de-sistemas}"
      --tiempo ${TIEMPO:-60}
      --workers ${WORKERS:-2}

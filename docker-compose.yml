services:
  skillmap:
    build: .
    volumes:
      - ./output:/app/output
    command: >
      --url "${SEED_URL:-https://co.computrabajo.com/trabajo-de-programador}"
      --tiempo ${TIEMPO:-60}
      --workers ${WORKERS:-4}